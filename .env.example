# Copy to .env for local development.
# Never commit real keys.

# Target mapping (current):
#   main / Vercel Production → https://heciguajwmutdejgdujc.supabase.co  (fresh prod)
#   dev  / Vercel Preview    → https://cedpnxjyjhplmigitfso.supabase.co  (existing = dev)
#   local .env               → same as DEV (never prod customer DB)

# --- DEV (local + Vercel Preview / Development) ---
VITE_SUPABASE_URL=https://cedpnxjyjhplmigitfso.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR-DEV-ANON-OR-PUBLISHABLE-KEY

# AI column understanding (set as a Supabase Edge Function secret, NOT in VITE_):
#   supabase secrets set GEMINI_API_KEY=your-key
#   # or: supabase secrets set OPENAI_API_KEY=your-key
# Used once per upload when there is no converted/won column.

# --- PROD (Vercel Production only) ---
# VITE_SUPABASE_URL=https://heciguajwmutdejgdujc.supabase.co
# VITE_SUPABASE_ANON_KEY=<from Vercel Production env>
